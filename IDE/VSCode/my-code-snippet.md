- javascriptreact, typescriptreact

```
"Print to console": {
		"prefix": "mmap",
		"body": [
			"{data.map((item, index) => {",
			"  return (",
			"      <div key={item.id}>",
			"",
			"      </div>",
			"  )",
			"})}"
		],
		"description": "react map function"
	}
```
